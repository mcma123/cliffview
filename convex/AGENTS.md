# convex

## Purpose

The Convex backend for the academy: schema, server functions, and the deployment-target contract. Schema and the admin read surface are live on prod; the React app does not consume them yet.

## Ownership

- Owns everything under `convex/`, the deployment target, and the Convex CLI workflow
- Owns the `CONVEX_DEPLOYMENT`, `VITE_CONVEX_URL`, and `VITE_CONVEX_SITE_URL` values in `.env.local`
- Does not own the app's data-access boundary — `src/infrastructure` owns how the app reaches a backend

## Local Contracts

Deployment target, the standing rule:

- This project works against **production `diligent-mink-756`** directly. No dev deployment is part of the workflow
- `CONVEX_DEPLOYMENT=prod:diligent-mink-756` in `.env.local` is the single source of truth. `npx convex deployment select` rejects a prod target ("Selecting a production deployment is unsupported") — never try to persist it that way
- Team `mcmarsh-fif-gmail-com`, project `cliefview-school`. The retired personal dev deployment was `striped-hyena-169`; do not reintroduce it
- Develop live against prod with `npx convex dev --prod`. `--prod` is a hidden flag and does not appear in `npx convex dev --help`. Plain `npx convex dev` reconfigures a _dev_ deployment — do not run it
- Per-command override when needed: `--deployment diligent-mink-756`
- `npx convex deploy` targets prod regardless of the env var

Because prod is the working target, ordinary commands are production actions:

- Announce the target before any deployment-affecting command: `target: prod (diligent-mink-756, project default production)`
- Get a fresh explicit yes in the current session before `npx convex dev --prod`, `npx convex deploy`, `npx convex env set` / `env remove`, a mutating `npx convex run`, `npx convex import`, or `npx convex export`. Consent never carries across actions or sessions
- Safe without asking, read-only: `npx convex env list`, `npx convex data`, `npx convex logs`, `npx convex function-spec`, `npx convex insights`
- Convex MCP: default to a non-prod selector. A read-only prod audit passes `--cautiously-allow-production-pii` alone; `--dangerously-enable-production-deployments` only when the user asks to change prod in that session. Never pair them by default
- Read-only mode, once the user requests it, is absolute for the session
- There is no dev sandbox in front of prod: the first push applies schema changes to live data, and a destructive schema change can drop it. Rehearse risky schema or migration work on a preview deployment first
- A push that "changed nothing" means the wrong deployment was hit — re-check the target, do not re-run the push

Code rules:

- `convex/_generated/` is generated — never hand-edit. Regenerated by `npx convex dev --prod` and `npx convex codegen`
- `convex/_generated/ai/guidelines.md` is Convex's own function-authoring guidance (targets `^1.44.0`); follow it when writing functions here
- `npx convex codegen` regenerates `_generated` **without** changing the deployed function surface, despite printing an "Uploading functions" line. Use it after adding a function so `api.*` types resolve; it is not a push and needs no gate
- Installed version is `convex@^1.45.0`

Data model — 20 tables:

- Content: `modules`, `moduleObjectives`, `lessons`, `assets`, `lessonAssets`, `assessmentQuestions`, `assessmentQuestionOptions`
- People: `phases`, `users`, `enrollments`, `lessonProgress`, `assessmentAttempts`
- AI review: `aiGenerations`, `aiQuestions`, `aiQuestionOptions`, `aiReviewDecisions`
- Analytics: `progressEvents`, `monthlyRollups`, `counters`, `auditLog`

Schema rules that must hold:

- **Content and per-learner state are different tables.** Never put progress, completion, or a learner cursor on a content row. The old seed stored one hypothetical learner's `progressPercent` / `isCurrent` / `isComplete` on the shared module and section, which is why a lesson's editorial publish state was computed from whether a demo user had finished it
- **Nothing derivable is stored.** Lesson counts, asset counts, compliance percentages and averages are computed from indexed rows, or read from `counters` when the source table grows without bound. Do not re-add a `sectionCount`-style field: the original disagreed with the real lesson list on seven of nine modules and nothing recomputed it
- **Editorial state is `publishState` (`draft | published | archived`)** on modules, lessons, and assets. `locked` is never stored — it is derived at read time from `modules.sequence` plus the caller's enrollments
- **Ordering** lives in `order` (and `modules.sequence`), always read through a `by_<parent>_and_order` index so the database returns sorted rows. Reorders are a dense 1..N renumber inside one transaction via `convex/lib/ordering.ts`
- **Addressing:** modules by `slug`; lessons by module-scoped `slug` (`by_moduleId_and_slug`) so existing learner URLs survive — lesson slugs are NOT globally unique; assets and users by `Id<>`
- **Timestamps, not prose.** `contentUpdatedAt`, `lastActiveAt`, `lastAccessedAt`, `durationMinutes`, `monthlyRollups.monthKey` (`"YYYY-MM"`). All display formatting belongs in `src/application`
- **`auditFields` is just `contentUpdatedAt`.** "Who changed this" lives in `auditLog`; do not add `createdBy`/`updatedBy` back onto content rows — it duplicates the audit trail and would return an admin identity string from every public read
- Index names list every field in order: `["moduleId", "order"]` becomes `by_moduleId_and_order`
- Never read the wall clock in a query. `dashboard.adminOverview` takes `now` as an argument

Mutations:

- Every one calls `requireAdmin(ctx)` **first**, before reading or writing anything, and stamps `contentUpdatedAt` via `stamp()`. Who acted goes to `auditLog` through `recordAudit`, never onto the content row
- Note the ordering: Convex validates `args` _before_ the handler runs, so a malformed call fails validation without ever reaching the authz check. That is safe (nothing executes) but it means a validation error is not evidence that the gate works — test the gate with valid args
- `modules.publish` is the only way to publish a module, and it checks readiness: non-empty title, description, audience and outcome, a pass mark in range, and at least one published lesson. `setPublishState` deliberately refuses to publish so it cannot be used as a way around the gate
- Deletion is narrow on purpose. `modules.remove` refuses once anyone is enrolled — that is what `archived` is for — and otherwise cascades to lessons, their join rows, assets and objectives. `assets.remove` clears all three kinds of reference to the asset: join rows, any lesson using it as hero media, and the module hero pointer
- **Every path that drops an asset row deletes its blob first**, through `deleteBlobIfPresent` in `convex/lib/storage.ts`. A row deleted on its own leaves an object in the bucket that nothing can reach or name again — unreferenced, unlistable from the app, and still billed. That covers `assets.remove`, the `modules.remove` cascade, `assets.detachFile`, and replacement inside `assets.attachFile`
- Ordering stays a dense 1..N. Inserts append via `nextXOrder`, deletes call `renumberX`, `lessons.move` swaps two adjacent rows in one transaction, and `reorder` validates the id set against the authoritative siblings with `assertSameMembers` so a stale client fails loudly instead of dropping a row
- Slug uniqueness is a mutation invariant, since Convex has no unique constraint: modules probe `by_slug` globally, lessons probe `by_moduleId_and_slug` **within their module only** — lesson slugs are deliberately not globally unique
- Cross-parent writes are refused: an asset can only be attached to a lesson in its own module, and a module hero must belong to that module

File storage:

- Files live in **Cloudflare R2**, not Convex's built-in storage. `assets.r2Key` and `aiGenerations.r2Key` are opaque object keys — plain strings, not `Id<"_storage">`. Phase 1 guessed built-in storage and declared `storageId: v.id("_storage")` early to avoid a schema change; the rename to `r2Key` in Phase 6 is what consent gate G8 actually covered
- One `R2` instance for the whole backend, in `convex/lib/storage.ts`. `modules.remove` cascades into blobs, so a second `new R2(...)` would be a second place to keep configured
- The upload is three legs: `assets.generateUploadUrl` (admin-gated, returns a server-issued uuid key), a direct browser `PUT` to R2 that never passes through Convex, then `assets.attachFile` to point the row at the key. Between leg two and leg three the object is an orphan, which is why a failed `attachFile` is surfaced rather than swallowed
- The key is **always server-issued**. `clientApi`'s `generateUploadUrl` takes no arguments, so a caller cannot name a key and therefore cannot aim an upload at another asset's blob. `attachFile` additionally refuses a key already held by a different asset, via `by_r2Key`: two assets sharing one blob would mean deleting either breaks the other
- `contentType` and `sizeBytes` reaching `attachFile` are the **browser's report**, treated as a first guess so the editor shows a correct size immediately. `assets.applySyncedMetadata` overwrites both with what R2 reports. It has to work this way: the client-facing `syncMetadata` is a mutation that only _schedules_ R2's HEAD request, so `r2.getMetadata` is still empty while the upload is being recorded
- **The bucket's CORS policy is part of the deploy, not a nicety.** The browser PUTs straight to R2, so an origin missing from the policy fails with an opaque network error and nothing appears in the Convex logs. Allowed origins are the localhost dev ports (3000, 5173, 8080-8085) plus the deployed frontend origin `https://cliffview.cyphersoftai.com`. Methods are `PUT` and `GET`, the only allowed request header is `content-type`, and `ETag` is exposed. **Any new origin the app is served from has to be added here or every upload from it breaks** — silently, with nothing in the Convex logs. Read the policy back with `GET /accounts/<id>/r2/buckets/<bucket>/cors`
- The bucket is **`cliffview`**, not the `cliffview-academy-assets` the Phase 6 plan named: a bucket already existed when the credentials were made, and a second one would have split the same project's files across two buckets
- The size cap lives in `convex/lib/storage.ts` and is duplicated in `src/hooks/use-asset-upload.ts`. A presigned PUT cannot be size-capped, so the browser check only saves a doomed round trip and the refusal that counts happens after the bytes land. Change both together
- Replacing a file clears `pageCount` and `durationSeconds`. Nothing recomputes them, so a new file would otherwise be described with the old file's numbers

Function surface:

- Public queries, all `requireAdmin` except `auth.viewer`: `modules.listForAdmin`, `modules.adminDetail`, `lessons.adminDetail`, `assets.adminDetail`, `dashboard.adminOverview`, `staff.directory`, `staff.detail`, `questions.adminList`, `reports.compliance`
- Public mutations, all `requireAdmin`: `modules.{create,update,publish,setPublishState,remove}`, `objectives.{add,update,remove,reorder}`, `questions.{save,remove,reorder}`, `lessons.{create,update,setPublishState,move,reorder,remove,attachAsset,detachAsset}`, `staff.{create,update,setEmploymentStatus,assignModules,unassignModule}`, and the actions `invites.{preview,accept,resend}`, `assets.{create,update,setPublishState,remove,attachFile,detachFile}`, and `assets.{generateUploadUrl,syncMetadata}` generated by the R2 component's `clientApi`
- `assets.generateUploadUrl` is gated through the component's `checkUpload` callback rather than a handler body, which is the one place `requireAdmin` is not the first line of a mutation we wrote. It still runs before anything else. An ungated upload-URL mutation lets anyone fill the bucket, so this gate is load-bearing
- `auth.viewer` is public and ungated by design: it returns the caller's own name, role and job title, or null when unauthenticated, because the admin gate calls it to decide what to draw
- Public actions owned by Convex Auth: `auth.signIn`, `auth.signOut`, plus `auth.isAuthenticated`
- Internal: `auth.allowAdminClaim`, `auth.claimStatus`, `auth.provisionAdmin`, `auth.store`, `seed.run`, `assets.applySyncedMetadata`, `assets.onSyncMetadata`
- `auth.provisionAdmin` is the only function that can mint an administrator, which is why it is internal and why it stays internal. It creates or repairs an operator row, sets `accessRole: "super_admin"`, and opens the single-use claim window in one call. Idempotent: re-running repairs role and status instead of inserting a second person
- `assets.applySyncedMetadata` is the only mutation without a `requireAdmin` call, and deliberately: it is an `internalMutation` the R2 component invokes with no end-user identity attached, and it is unreachable from a client. It also does not `stamp()` — a background metadata correction must not move `contentUpdatedAt` and make "Last edited" jump
- Helpers in `convex/lib/`: `authz.ts` (`getActor`, `requireStaff`, `requireAdmin`), `ordering.ts`, `audit.ts`, `counts.ts`, `time.ts`, `progress.ts` (`applyLessonCompletion`, `publishedLessons`), `assessments.ts` (the shared publish gate), `credentials.ts` (`passwordAccountFor`, `rekeyPasswordAccount`), `storage.ts` (the single R2 client, the URL TTL, the size cap, `deleteBlobIfPresent`), `email.ts` (the single Resend client, the FROM address, the undeliverable-domain guard, `sendInvitationEmail`), `invitationEmail.ts` (pure copy, no imports, so it is testable with zero setup), `invites.ts` (token minting, hashing, `consumeInviteOrThrow`)
- Shared validators in `convex/validators.ts`, mirroring the unions in `src/domain/academy/entities.ts`

Awards and the leaderboard (`convex/lib/awards.ts`, `convex/learn.ts`):

- **Awards are the one thing here that is incremented, not recomputed**, and that is deliberate: XP and CPTD points record what somebody was paid at the time, not a function of their current state. Recomputing them would un-pay a teacher whose module was later archived. What keeps the increment honest is that `recordLessonProgress` captures the prior status _before_ writing anything and pays only on the transition into completed — replaying a finished lesson pays nothing
- **What work is worth lives in one module.** `XP_PER_LESSON`, `xpForModule`, the badge catalogue and the streak rule are all in `convex/lib/awards.ts`, pure and database-free, so the mutation that pays out and the query that explains the total cannot disagree
- **Every badge must be answerable from data the app records.** The list this replaced contained "Mentor — help 3 colleagues", which no table could ever answer, so it would have stayed locked forever while looking like something you could work towards
- **`badgeAwards` is a deliberate exception to "nothing derivable is stored".** A three-day-streak badge must stay earned after the streak lapses, and "when did I earn this" is derivable from nothing
- **`progressEvents` is written for the first time here**, and it is the only per-user activity history that exists — a streak is computable only because of it. Nothing backfills it, so every streak genuinely starts at 1
- **`learn.leaderboard` is the first query to return one colleague's data to another.** The personnel file stays `requireAdmin`; what makes this defensible is how narrow it is. It returns name, initials, phase name and `xpTotal` and is hand-shaped rather than `schema.doc("users")` precisely so it cannot drift into returning email, `employmentStatus`, `jobTitle`, `compliancePercent` — the school's performance metric — or an `Id<"users">` that would be a key into the rest of the record. `super_admin` is excluded for the same reason `dashboard.adminOverview` excludes it. A test asserts the returned keys, so widening the shape breaks the build rather than quietly leaking

The learner surface (`convex/learn.ts`):

- **The first and only `requireStaff` code in the backend.** Everything else is `requireAdmin`, deliberately, because it exposes drafts, archived content and the whole asset list. `learn.ts` exposes published content the caller has been assigned, and their own progress
- **An enrollment is the entitlement.** `assignedModuleOrThrow` refuses a module the caller has no `enrollments` row for, and gives the same message for "not assigned" as for "not published" — whether a draft exists is editorial information a learner should not be able to infer
- **Identity is never an argument.** `requireStaff` reads it from the verified token, so nobody can request another person's training record by passing their id
- **A module video is `modules.featuredAssetId`, not a lesson attachment.** The two are separate pointers; a hero video is reachable from `learn.moduleDetail` and nowhere else. Reading only `lessonAssets` is why an uploaded video can appear to vanish
  Assessments (`convex/questions.ts`, `convex/lib/assessments.ts`, `learn.{assessment,submitAssessment}`):

- **A question and all of its options are written in one mutation.** `questions.save` is the one place this backend diverges from the `objectives.ts` row-at-a-time shape, and the reason is the invariant: exactly one option is correct. Per-option mutations would leave reachable instants where zero or two were, and the learner query reads live data on a published module — so "reachable" means a real teacher, mid-edit. The cost accepted is last-write-wins over the whole question
- **The answer key never leaves the server.** `learn.assessment`'s option shape is spelled out field by field in its `returns` validator and is never `schema.doc("assessmentQuestionOptions")`, which would pass `isCorrect` straight into every learner's page payload. Same reason `learn.leaderboard` is hand-shaped. A test asserts the exact returned key set, so widening it breaks the build rather than quietly leaking
- **Grading reads the database, never the client's claim.** The score's denominator is always the whole bank, so submitting only the question you know scores 1/N rather than 100%. An unanswered question or an option id belonging to another question scores wrong rather than throwing — a legitimate submission and a client bug respectively, neither worth a 500 in a teacher's face. A zero-question assessment is refused outright, because 0/0 would round to 100% and mint a Quiz Ace badge
- **`recordLessonProgress` refuses `completed: true` on a lesson of kind `assessment`** (`USE_ASSESSMENT`). It is a public mutation, so hiding the button would have left the pass mark advisory: anyone could take the module, its XP and its CPTD points without answering a question. `completed: false` still records a visit
- **A published assessment lesson with an empty bank is refused at both doors** — `modules.publish` and `lessons.setPublishState`, sharing `assertAssessmentReady`/`hasQuestions` in `convex/lib/assessments.ts`. Gating only the first is decorative: publish the module while the lesson is a draft, then publish the lesson. There is deliberately no minimum question count; that is the school's judgement
- **`enrollments.score` is updated on every attempt, not only on a pass.** The schema calls an undefined score "never attempted", which is a fact about attempts — somebody who has scored 70% five times should not read as "—" on their own profile. This is also the first real writer of that field, which is what finally makes the `quiz_ace` badge earnable
- Per-attempt answers are **not** stored. The results screen is the mutation's return value, so nothing reads them; the trade is that the per-question breakdown is lost on refresh and the page degrades to a best-score banner. Note the coupling if that is ever revisited: `save` replaces option rows wholesale, so option ids are not stable across an edit and an answer row would point at a deleted option

The learner surface, continued:

- `learn.recordLessonProgress` writes `lessonProgress`, then **recomputes** `enrollments.progressPercent` and `users.compliancePercent` from the rows rather than incrementing them — the same formula as `staff.recomputeCompliance` and the seed. Completion is sticky: re-opening a finished lesson is a visit, not a regression

Invitations (`convex/invites.ts`, `convex/lib/invites.ts`):

- The prod push installs **six** components for Resend, not four: `resend`,
  `resend/rateLimiter`, `resend/emailWorkpool`, `resend/callbackWorkpool`, and a
  `batchWorker` nested inside each workpool. The test harness registers the first four.
  That is enough only because no test drains the scheduler — the batch workers are
  reached at delivery time, which never happens offline. If a test ever needs
  `finishInProgressScheduledFunctions`, register
  `resend/emailWorkpool/batchWorker` and `resend/callbackWorkpool/batchWorker` too,
  and expect a real send attempt

- **An invitation is the only way a teacher sets a first password.** Before this existed, `createOrUpdateUser` demanded a claim window only for privileged roles, so a `staff` row had no window and no token and whoever submitted `flow: "signUp"` first for a guessable school address became that teacher. The fourth refusal in that callback closes it
- **`Password` must stay uncalled** in `convex/auth.ts`. `invites.accept` passes `{ email, inviteTokenHash }` to `createAccount`, and the callback trusts that key _because_ the provider's `defaultProfile` builds `{ email }` from scratch and discards every other submitted field — there is no client path that can inject it. Configuring `Password({ profile })` would create one, and would also expose `emailVerified`/`phoneVerified`, which Convex Auth patches straight onto `authAccounts`
- **Only the SHA-256 of a token is stored.** Hashing happens **in actions only**: `crypto.subtle` is not documented for the query/mutation runtime, and Convex Auth itself uses a pure-JS SHA-256 in its mutation path rather than relying on it
- **The invite is spent inside `createOrUpdateUser`**, in the same transaction as the `authAccounts` insert. Validating in an action and consuming in a separate mutation would leave a window where the account exists and the link is still live
- `createAccount` **bypasses `validatePasswordRequirements`**, so `invites.accept` enforces the 8-character minimum itself. It also mints no session, which is why redeeming leaves the teacher signed out and sent to the sign-in page
- Admins are never invited by email. `smt_admin` still claims through `internal.auth.allowAdminClaim`, because a seven-day emailed link to an admin row is a takeover path
- Seeded staff on `cliffview.example` can never receive mail (RFC 2606). `invites.resend` returns the link so an admin can hand it over directly. Those four are demo fixtures rather than colleagues and are being deactivated; `cliffview.example` stays as the bare-username fallback until the school's real domain is settled, and note the domain gates **nothing** beyond that convenience — a full address on any domain has always worked

Authentication and authorization:

- **Convex Auth** (`@convex-dev/auth`, email + password). It is a library, not a Convex component, so it needs no `convex.config.ts` entry — its tables are spread into our own schema via `...authTables`
- `convex/auth.config.ts` sets the JWT issuer to `process.env.CONVEX_SITE_URL`. If this file is missing or its domain is wrong, `getUserIdentity()` returns null forever and the app is silently always-signed-out with no error anywhere. Never hardcode the value, and never declare `CONVEX_SITE_URL` in `convex.config.ts`
- **`users` is both the staff profile and the auth user table.** `createOrUpdateUser` in `convex/auth.ts` never inserts — it resolves a sign-in to a pre-provisioned row by email and returns that id, which is what lets `users` keep its required fields
- **`convex/auth.ts` is the whole sign-in security model.** Every rule about who may sign in lives in that one callback: an unknown email is refused so nobody self-registers into the school; an inactive profile is refused; and a privileged profile (`smt_admin`, `super_admin`) is refused unless an operator has opened a single-use window with `internal.auth.allowAdminClaim`. Add rules there, not in the UI
- Admin provisioning: `npx convex run auth:allowAdminClaim '{"email":"...","minutes":30}'`, then the admin signs up with that email inside the window. `auth:claimStatus` reports provisioning state. Both are internal
- Authorization goes through `convex/lib/authz.ts` only: `getActor` resolves identity via `getAuthUserId` (never a client-supplied id, never a parsed token string), `requireStaff` demands an active profile, `requireAdmin` demands an admin role
- Every admin query calls `requireAdmin` first. The four content queries are admin-gated rather than staff-gated because they expose draft and archived content; learner-facing content queries arrive in Phase 8 and will filter on `publishState` instead
- `auth.viewer` returns null rather than throwing when unauthenticated, because the UI gate uses it to decide what to draw. It exposes only the caller's own name, initials, job title and role
- Every mutation calls `requireAdmin` first, from its first commit
- A signed download URL may leave a query only if that query is **gated and scoped to something the caller is entitled to**, and it is **never written to a row**. It is an expiring credential, not an address. `assets.adminDetail` is `requireAdmin`; `learn.moduleDetail` and `learn.lesson` are `requireStaff` **plus** `assignedModuleOrThrow`, so a learner is only ever handed a URL for media inside a module they have been assigned. This rule used to say `requireAdmin`-gated only — written when nothing was gated at all and admins were the only readers, which would have made it impossible for a teacher to watch the video they were assigned
- Prod environment variables: `JWT_PRIVATE_KEY`, `JWKS`, `SITE_URL`, `RESEND_API_KEY`, `RESEND_TEST_MODE`, plus the five R2 credentials (`R2_BUCKET`, `R2_ENDPOINT`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_TOKEN`). No R2 value may ever be given a `VITE_` name — that would ship the secret in the browser bundle. Generate the keys **headlessly** with `jose` — the interactive `npx @convex-dev/auth` wizard needs a TTY and hangs in a non-interactive session — and set them with the `NAME=VALUE` form (`npx convex env set "JWT_PRIVATE_KEY=$JWT"`), never `env set NAME "$VAL"`, because the key starts with `-----BEGIN` and the CLI reads the leading dash as a flag. `SITE_URL` is `https://cliffview.cyphersoftai.com`, the deployed frontend origin. It was `http://localhost:8081` until the frontend shipped. There is one deployment, so there is one `SITE_URL`: pointing it at the deployed origin is what makes auth redirects work there, and it is why local dev is no longer the origin Convex Auth considers canonical

Seed contract (`convex/seed.ts`, `convex/seed/data.ts`):

- `internal.seed.run` is an **idempotent `internalMutation`**, not `npx convex import`. The seed performs cleanup rather than transcription, and slug-to-id resolution is natural in a mutation
- Guards: `internalMutation` (unreachable from any client), a required `confirm: "cliffview"` argument, and `mode: "reset"` which refuses populated tables unless `iAmSure: true` is also passed
- Idempotency keys are natural keys: phase order, user email, module slug, (moduleId, lesson slug), (moduleId, asset title). Re-running `insert-missing` inserts nothing
- `convex/seed/data.ts` owns the nine module slugs and all seed prose, and is now the **only** copy. It began as a deliberate duplicate of an in-memory repository in `src/infrastructure`, which has since been deleted — so the standing warning to change both is resolved
- **Staff records are `requireAdmin`, never `requireStaff`.** `staff.directory` and `staff.detail` return names, email addresses, employment status and compliance history for the whole school. Being signed in as a teacher is not permission to read the personnel file
- **`accessRole` is capped at `smt_admin` from the console.** `staff.{create,update}` refuse `super_admin`, so no client-reachable path can mint an operator — that stays `internal.auth.provisionAdmin`
- **Two lockout guards.** Nobody may change their own `accessRole` or deactivate their own account. Without them an admin can demote themselves out of the console and, if they were the last one, lock the school out of its own admin surface
- **An enrollment row _is_ the assignment.** There is no join table, so `staff.assignModules` is the only way a module reaches somebody's tracker outside the seed. It is idempotent — re-assigning a module a person already has is a no-op, not a second row, which matters because `compliancePercent` is the mean over those rows and a duplicate would be counted twice. Only **published** modules can be assigned: a draft is unfinished work nobody can do, and enrolling anyone in one also makes the module undeletable, since `modules.remove` refuses once there are enrollments
- **`staff.unassignModule` only removes an untouched assignment**, and checks `lessonProgress` as well as the enrollment row — an enrollment can read as untouched while the person has in fact opened lessons. Once somebody has started, the row is their training record, the same reason deactivation is the delete for a profile
- **An address is two facts in two tables, and they move together.** `users.email` is what this app resolves a sign-in against; `authAccounts.providerAccountId` is what Convex Auth resolves it against, and it runs first. `staff.update` patched only the first, so correcting a typo locked somebody out under their new address while the old one kept working — and the old one resolves straight to `existingAccount.userId`, skipping every refusal in `createOrUpdateUser`. `rekeyPasswordAccount` in `convex/lib/credentials.ts` moves the credential in the same transaction, leaving the secret alone, and `revokeLiveInvites` stamps the invitations that `classifyInvite` would have refused anyway. Guarded on a real change: re-keying a no-op edit would revoke a link somebody is part-way through redeeming
- **Deactivation is not a sign-in refusal**, and the comment that used to say so was half right. `createOrUpdateUser` runs on sign-**up**; a returning sign-in resolves from `authAccounts` and never reaches it, so an inactive person can still obtain a token. What ends their access is `requireStaff`, which every gated read calls — they hold a session and can see nothing with it. Pinned by a test in `convex/invites.test.ts`
- **Deactivation is the delete.** There is no `staff.remove`. A `users` row is referenced by enrollments, lesson progress, assessment attempts and the audit log, and it is the identity a Convex Auth account is bound to, so deleting it would orphan a person's whole training record and leave a live session pointing at nothing. `employmentStatus: "inactive"` already ends access — both `requireStaff` and `createOrUpdateUser` refuse an inactive account
- **`users.email` uniqueness is a mutation invariant**, like module slugs. It is how `createOrUpdateUser` resolves a sign-in to a person, so a duplicate would make that ambiguous and start throwing on `.unique()` for everyone
- **`compliancePercent` is recomputed, never typed.** `staff.update` re-derives it from the enrollment rows as the mean of `progressPercent` — **exactly** the formula `convex/seed.ts` used. If the two disagreed, the first edit of a seeded profile would silently rewrite a number that was already correct. `cptdPoints` and `xpTotal` are likewise earned and have no input
- **`super_admin` means operator login, not staff.** `dashboard.adminOverview` leaves those rows out of headcount, the school-wide compliance average and the per-phase averages. An operator account belongs to nobody, has no modules and can earn no CPTD points, so counting one would add a person who does not exist to the directory's headcount and drag compliance towards zero — three tiles nobody could reconcile against the staff list. `smt_admin` is a real person with a job title and still counts. `staff.directory` applies the same exclusion, and `staff.detail`/`update`/`setEmploymentStatus` refuse an operator id outright
- `users.phaseId` is required, so `provisionAdmin` parks an operator in the first phase. That is only defensible because the phase averages exclude it; if that exclusion is ever removed, the placeholder becomes a lie
- The seed **derives** `users.compliancePercent` and the `counters` values from the enrollment rows it writes. Never seed an aggregate directly — that is how the old numbers came to contradict their own rows
- `monthlyRollups` are anchored to the month the seed runs, and are now **written but never read** — the dashboard stopped reading them once the trend moved to `enrollments.completedAt`. What populates the six-month window today is the spread of `lastAccessedAgoMs` across the seeded completed enrollments, which is what `completedAt` is derived from
- Take a snapshot before any schema-affecting push from here on: `npx convex export --path backups/<name>.zip`. `backups/` is gitignored — snapshots hold real staff data and must never be committed

Current state:

- Every route reads Convex directly. The legacy container and in-memory repository are gone, deleted with the AI review screen that was their last caller
- **`reports.compliance` and the overview's "Modules Completed" tile now count the same thing the same way, and `convex/dashboard.test.ts` holds them to it.** The tile used to read `COUNTER.completedModules`, which only the seed ever wrote — `applyLessonCompletion` completes modules and emits a `module_completed` event but never called `bumpCounter` — so the counter froze at its seeded value and drifted further with every real completion. It was resolved the way that note recommended: recomputed from `enrollments`, in the same traversal and over the same scope (`activeStaff`) as the report, rather than by adding the bump, which would have made it less obviously wrong without making it right. Neither `COUNTER.completedModules` nor `monthlyRollups` is read anywhere now; both still exist and are still seeded, and removing them is schema-affecting cleanup awaiting a snapshot
- **Reports carries no history on purpose, and neither does the overview any more.** `monthlyRollups.averageCompliancePercent` is `60 + i * 3` from the seed and no cron maintains it, so the dashboard's month-over-month delta was that literal subtracted from a live figure — a regression that never happened. The delta is gone rather than faked; a real one needs a cron writing genuine snapshots first. The six-month trend is now bucketed from `enrollments.completedAt`, which is written by the seed and by `applyLessonCompletion` alike, so it is real on seeded data and moves on live completions. Nothing synthesised may reach a document an admin forwards
- Prod holds seeded data: 5 phases, 4 users, 9 modules, 44 lessons, 36 assets, 55 lesson-asset links, 20 enrollments, 2 AI questions, 6 monthly rollups
- `aiGenerations` and `aiReviewDecisions` have live writers as of the generation pipeline — `aiReview.generateFromAsset` opens a generation row _before_ the model call so a failure has somewhere to be recorded, and `aiReviewQueue.setDecision` appends a decision. They, `lessonProgress`, `assessmentAttempts`, `progressEvents`, `badgeAwards` and `auditLog` are merely **unseeded**: they fill from real traffic only, and nothing backfills them. That is why Reports is built on `enrollments` and avoids them — a lesson-level figure would contradict the module-level one for every seeded enrollment, which reports its progress with no `lessonProgress` rows behind it
- `convex/convex.config.ts` exists as of Phase 6 and registers exactly one component, `@convex-dev/r2`. Phases 1 and 4 recorded that no such file was needed, which was true of Convex Auth (a library, spread in via `...authTables`) and false of R2. Declaring it switches codegen into component mode, so `npx convex codegen` now contacts the deployment to analyse components and cannot run fully offline — that is an analysis round trip, not a deploy
- R2 pulls in `@convex-dev/action-retrier` as a nested component and registers it itself. Do not add it to `convex.config.ts`
- `aiReviewQueue.queue` returns `modules` alongside `sources` so the AI review screen can upload into a module without a second query. `sources` is the narrower list — only assets that are a `document` or `worksheet` **and** carry an `r2Key` — because those are the only ones there is anything to read. A module with no readable asset still appears in `modules`: you have to be able to upload the first one

## Work Guidance

- Prefer the bundled `convex-*` skills over memory: `convex-design` for schema and functions, `convex-deploy-guard` before any deployment-affecting command, `convex-docs` for version-current API details, `convex-reviewer` and `convex-authz` before shipping
- Write schema and functions, typecheck, then ask for consent to push — in that order
- **Convex is the port.** There is no hand-written repository in front of it: route loaders call `convexQuery`, route components call `useSuspenseQuery`, and `src/application` presenters turn documents into view-models. Shared components in `src/components` stay prop-driven and never import Convex. Decision and phasing: `ADMIN_BACKEND.md`

## Verification

- `npm test` runs the `convex-test` suites in `convex/*.test.ts`. This is the substitute for a dev deployment: authorization is the thing that cannot be verified by clicking, so **every mutation ships with the four authz negatives** — no identity, a staff identity, an inactive account, and a cross-parent write. Run it before asking for consent to push, not after
- In tests, `t.withIdentity({ subject: userId })` is enough: `getAuthUserId` splits the subject on `"|"` and takes the first part
- Component tests need both components registered by hand. Each test file has a local `newTest()` that calls `r2Component.register(t)` **and** `actionRetrier.register(t, "r2/actionRetrier")`. The second call is not redundant: R2's own helper registers the retrier under the bare name `actionRetrier`, but at runtime it is addressed by its nested path, so without it any mutation that deletes a blob fails on an unregistered component
- R2 credentials for tests are fake values in `vitest.config.ts`, set via `test.env` because the component reads `process.env` in its constructor, before any test body runs. Presigning is local HMAC work, so `generateUploadUrl` and `getUrl` are fully testable offline. `deleteObject` only enqueues a scheduled action, so never call `finishInProgressScheduledFunctions` in a storage test unless you intend a real R2 call. **The same hazard, worse, for email:** `resend.sendEmail` only enqueues and a workpool delivers, so draining the scheduler in any test that creates staff would attempt a real send. `staff.create` schedules `internal.invites.deliver`, so that is most of them
- Confirm the live target: `npx convex env list` names the deployment it reached — expect `on prod deployment diligent-mink-756`
- Typecheck this folder with `npm run typecheck:convex` (`tsc --noEmit -p convex`) — zero production contact. `convex/tsconfig.json` holds the convex@1.45 CLI template shape: `target: ESNext`, `lib: [ES2023, dom]`, `module: ESNext`, and `exclude: ["./_generated"]`. The root `tsconfig.json` still covers only `src/`, so `npm run typecheck` does **not** check this code — run both
- Always typecheck before asking for consent to push. Do not rely on `npx convex dev --prod --typecheck enable`, which finds type errors only by pushing to production
- ESLint covers `convex/**/*.ts` (with `convex/_generated` ignored), so `npm run lint` applies here

## Child DOX Index

No children.
